#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    char s[n][n]; // s[rows][colms]

    int num = 0;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            cin >> s[i][j];
        }
    }
}