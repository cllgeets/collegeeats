#include<bits/stdc++.h>

using namespace std;  //the main thing in this question was analysing how we wre getting minimum.

int main(){
    int t; cin>>t;
    while (t--)
    {
    int n; cin>>n;
    int arr[n];
    for (int i = 0; i < n; i++)
    cin>>arr[i];
    
    cout<<"1 "<<n<<endl;
    }
}