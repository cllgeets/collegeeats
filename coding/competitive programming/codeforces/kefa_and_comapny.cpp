#include <bits/stdc++.h>

using namespace std;

int main()
{
    pair<int, int> a[100005];
    int n, d; cin>>n>>d;
    for (int i = 0; i < n; i++)
     cin>>a->first>>a->second;
    sort(a, a+n);
    

}