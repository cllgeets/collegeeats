sudo apt update -> updating ubuntu system

sudo apt install openssh-serveer openssh-client -> installing openssh in linux

sudo systemctl status ssh -> verifying status of ssh server

sudo ufw allow ssh -> opening a ssh port to allow ubuntu firewall

sudo ufw status -> checking firewall status

sudo ufw enable -> enabling firewall 

hostname -I -> checking IP address
